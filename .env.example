# Public URL of the ringdash API server.
# For local dev this is typically http://localhost:8899.
# For production set this to the Cloudflare Tunnel hostname for ringdash
# AND add that same origin to CORS_ORIGINS in the ringdash .env.
VITE_API_BASE_URL=http://localhost:8899
